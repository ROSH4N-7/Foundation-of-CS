1) docker exec -it mysql mysql -u root -p
- This executes mysql and after this we need to enter password.
2) Create database school_info;
- After executing that i created a database named school_info using create database command.
3) Create table info(
    StudentID INT,
    StudentName VARCHAR(50),
    Email VARCHAR(100),
    ClubName VARCHAR(50),
    ClubRoom VARCHAR(10),
    ClubMentor VARCHAR(50),
    JoinDate DATE,
    PRIMARY KEY (StudentID, ClubName)
);
