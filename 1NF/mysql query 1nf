1) Create database school_info;
- After executing that i created a database named school_info using create database command.

2) Create table info(
    StudentID INT,
    StudentName VARCHAR(50),
    Email VARCHAR(100),
    ClubName VARCHAR(50),
    ClubRoom VARCHAR(10),
    ClubMentor VARCHAR(50),
    JoinDate DATE,
    PRIMARY KEY (StudentID, ClubName));
- After creating database I created a table named info with the provided details using the create table command.

3) Insert into info (StudentID, StudentName, Email, ClubName, ClubRoom, ClubMentor, JoinDate) VALUES
(1, 'Asha', 'asha@email.com', 'Music Club', 'R101', 'Mr. Raman', '2024-01-10'), .....;
- Inserting data into info table using insert sql command.

4)Select * from info;
- Displaying the data inserted in table using select sql command.